spring:
  application:
    name: service-command
  profiles:
    active: dev
dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  protocol:
    id: tri
    name: tri
    port: -1
logging:
  pattern:
    dateformat: yyyy-MM-dd hh:mm:ss.SSS
cmd:
  name: "Copyright 2023 by Spc."
  version: "v3.0.0"
  powered-by: "Mirai | Spring | Dubbo"